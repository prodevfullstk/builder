import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter',
});

export const metadata: Metadata = {
  title: 'Opendork - AI-Powered Website Builder',
  description: 'Build production-ready websites with AI agents. Canvas mode, Multi-agent system, and Visual editing.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased bg-background-primary text-white`}>
        {children}
      </body>
    </html>
  );
}
